spring.application.name=OnlineCarServiceStation

# API Context Path - all endpoints will be prefixed with /api
#server.servlet.context-path=/api

#DB config
# spring.datasource.url=jdbc:mysql://localhost:3306/backend?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
# spring.datasource.username=root
# spring.datasource.password=root
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# spring.jpa.open-in-view=false
# spring.jpa.show-sql=true
# spring.jpa.hibernate.ddl-auto=update

# Database configuration - MUST use environment variables
# Create a .env file or set these as system environment variables
# See .env.example for template
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
stripe.secret.key=${STRIPE_SECRET_KEY}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration - MUST use environment variable
jwt.secret=${JWT_SECRET}
spring.datasource.hikari.max-lifetime=25000
spring.datasource.hikari.idle-timeout=10000
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.maximum-pool-size=5
#spring.datasource.url=jdbc:postgresql://ep-withered-mountain-a1nvj1re-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
#spring.datasource.username=neondb_owner
#spring.datasource.password=npg_odha0qQKW7SC

#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

#default server port 8080
#server.port=9090


